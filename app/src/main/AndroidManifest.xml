<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendancesystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AttendanceSystem">
        <activity android:name=".Student_Table_View"></activity>
        <activity
            android:name=".StudentPage"
            android:theme="@style/Theme.AttendanceSystem.NoActionBar" />
        <activity android:name=".FacultyAttendance" />
        <activity android:name=".Table_View" />
        <activity android:name=".StudentAttendance" />
        <activity android:name=".FacultyRegistration" />
        <activity android:name=".StudentRegistration" />
        <activity
            android:name=".AdminPage"
            android:label="@string/title_activity_admin_page"
            android:theme="@style/Theme.AttendanceSystem.NoActionBar" />
        <activity android:name=".LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>